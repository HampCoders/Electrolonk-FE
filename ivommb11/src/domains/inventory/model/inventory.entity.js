export class InventoryItem {
    constructor(id, serviceName, components) {
        this.id = id;
        this.serviceName = serviceName;
        this.components = components; // array de strings u objetos
    }
}
